%br
%h1 Edit Expense Report

= render 'form' 

%br

= button_to "Delete Expense Report", expense_report_path(@expense_report), method: :delete, data: { confirm: 'Are you sure you want to delete this report?' }, class: "btn btn-secondary btn-sm" 
