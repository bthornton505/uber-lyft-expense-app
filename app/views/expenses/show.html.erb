<br>
<h1>Expense Details</h1>

<ul>
  <li>Month: <%= @expense.expense_report.month %></li>
  <li>Cost: $<%= @expense.cost %></li>
</ul>

<h3>Expense Category</h3>

  <ul> <!-- need to change this so it shows all the categories -->
    <li><%= @expense.categories.last.name %></li>
  </ul>

<h4>Expense Comments</h4>

  <ul id="expense_comments"> <!-- need to change this so it shows all the comments -->
    <li><%= @expense.comments.last.description %></li>
    <%# <%= render partial: 'expense_comments' %> 
  </ul>

  <%= link_to "Add Comment", new_comment_path(:expense_id => @expense.id) %>
  <%# <%= render partial: 'new_comment', locals: { comment: @comment } %> %>


<br>
<%= link_to "Edit", edit_expense_report_expense_path(@expense_report.id, @expense), class: "btn btn-secondary btn-sm" %>
<%= link_to "Delete", expense_report_expense_path(@expense_report.id, @expense), method: :delete, data: { confirm: 'Are you sure you want to delete this expense?' }, class: "btn btn-secondary btn-sm" %>
<%= link_to "Back", expense_report_path(@expense_report), class: "btn btn-secondary btn-sm" %>
