<%= form_for([@expense_report, @expense]) do |f| %>
<%= f.hidden_field :expense_report_id %>

  <%= f.label :month, "Month:" %>
  <%= f.text_field :month %>

  <%= f.label :cost, "Cost:" %>
  <%= f.number_field :cost %>


  <%= f.fields_for :comments do |comment_builder| %>

    <%= comment_builder.collection_select(:category_id, @categories, :id, :name, {prompt: 'Select Category'}) %>

    <%= comment_builder.label :description %>
    <%= comment_builder.text_area :description %>

  <% end %>
  <!-- Need to add form for selecting a category -->
  <%# <%= render partial: "categories/form", locals: {category: @category} %>
  <!-- Then add collection select to go through already created categories -->

  <!-- This is a form to add a comment -->
  <%# <%= render partial: "comments/form", locals: {comment: @comment} %>


  <%= f.submit "Add Expense" %>

<% end %>
